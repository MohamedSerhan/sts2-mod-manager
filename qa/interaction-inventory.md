# Interaction Inventory

This is the issue #157 inventory for existing user-facing behavior. Each row is a user interaction path, the test owner that proves it, and the user-visible assertion that release managers can trust.

Status meanings:

- `Automated`: owned by a command that runs under CI, `npm run qa`, or the release gate.
- `Manual`: intentionally outside the current Rust, Vitest, or WebDriver layers. Manual rows must keep a reason and review date.

Required tag classes are checked by `npm run qa:matrix`: `global-shell`, `menu`, `dialog`, `profile-flow`, `mod-library`, `settings`, `error-state`, `empty-state`, and `large-list`.

## Interaction Inventory

| ID | Surface | Interaction | Tags | Automated owner | Release command | Status | Assertions trusted | Manual reason | Review date |
|---|---|---|---|---|---|---|---|---|---|
| I001 | App shell | Topbar Home, Modpacks, Library, and Settings navigation swaps the main view without losing the shell, and the optional left sidebar keeps the same nav set. | global-shell | `src/App.test.tsx` | `npm run qa:coverage` | Automated | Topbar navigation remains the default, left sidebar mode is opt-in, visible view content changes, and legacy Browse routes land in the correct tab. |  |  |
| I002 | App shell | Topbar profile chip opens the active-pack switcher and routes Add pack or Manage all to Modpacks. | global-shell, menu, profile-flow | `src/App.test.tsx`; `src/components/ProfileSwitcher.test.tsx` | `npm run qa:coverage` | Automated | Popover opens, Add pack focuses the Modpacks code bar, and Manage all opens the Modpacks view. |  |  |
| I003 | App shell | Help button opens and closes the slide-out help drawer. | global-shell, dialog | `src/App.test.tsx`; `src/components/HelpDrawer.test.tsx` | `npm run qa:coverage` | Automated | Drawer content appears from the topbar and the close affordance dismisses it. |  |  |
| I004 | App shell | Game-detection status routes to Settings General from both detected and missing states. | global-shell, settings, empty-state | `src/App.test.tsx`; `src/views/Settings.test.tsx` | `npm run qa:coverage` | Automated | Missing-game status is visible, detected status shows a good state, and clicks focus game settings. |  |  |
| I005 | App shell | Modded and vanilla launch buttons call the launch commands and surface launch failures. | global-shell, error-state | `src/App.test.tsx`; `src/components/LaunchSpinner.test.tsx` | `npm run qa:coverage` | Automated | Buttons invoke the right commands, disable while the game runs, and failure toasts show. |  |  |
| I006 | App shell | App update banner supports dismiss, browser download, install and restart, and error recovery. | global-shell, dialog, error-state | `src/App.test.tsx`; `src/components/AboutCard.test.tsx`; `scripts/release-windows-installer-policy.test.mjs` | `npm run qa:coverage`; `node --test scripts/release-windows-installer-policy.test.mjs` | Automated | Banner state changes are visible, update install routes through the backend pinned installer, the setup EXE corrects older 1.7 update launches that lack `/D=`, relaunch fallback remains wired, and errors produce toasts. |  |  |
| I007 | App shell | Custom titlebar window buttons and resize handles are wired without crashing when capabilities are unavailable. | global-shell, error-state | `src/App.test.tsx`; `src/hooks/useResizableSidebar.test.tsx` | `npm run qa:coverage` | Automated | Window controls render, resize persists, and capability failures are logged without breaking the app. |  |  |
| I008 | App shell | In-app drag/drop event installs supported zip files and rejects unsupported files. | global-shell, mod-library, error-state | `src/App.test.tsx` | `npm run qa:coverage` | Automated | Drop overlay appears, zip drops call install, unsupported files and install failures show localized errors, including game-version incompatibility warnings. |  |  |
| I009 | App shell | External links are routed through the backend opener. | global-shell, error-state | `src/App.test.tsx`; `src/hooks/useOpenFeedback.test.tsx` | `npm run qa:coverage` | Automated | Normal links call `open_external_url` and opener failures show a toast. |  |  |
| I010 | App shell | Deep links import, activate, sync, dedupe, and retry share codes through one smart router. | global-shell, dialog, profile-flow, error-state | `src/App.test.tsx`; `src/lib/shareImport.smart.test.tsx`; `src/lib/shareImport.test.ts` | `npm run qa:coverage` | Automated | Cold and warm URLs route to Home, confirm dialogs behave once, failures are visible, and canceled URLs can be retried. |  |  |
| I011 | Home | Empty or first-run CTAs route to Settings, Library, Modpacks, Browse Modpacks, or Create Modpack. | empty-state, settings, profile-flow | `src/App.test.tsx`; `src/views/Home.test.tsx` | `npm run qa:coverage` | Automated | Empty-state actions open their target surface and creator CTA opens the create wizard. |  |  |
| I012 | Home | Paste share code flow imports a modpack or resolves already-subscribed states. | profile-flow, dialog, error-state | `src/lib/shareImport.smart.test.tsx`; `src/App.test.tsx`; `src/views/Home.test.tsx` | `npm run qa:coverage` | Automated | Install, activate, reapply, synced, already-active, canceled, and malformed outcomes show the expected visible result. |  |  |
| I013 | Home | Subscription update cards let the user inspect and apply curator updates. | profile-flow, dialog, error-state | `src/views/Home.test.tsx`; `src/components/SubUpdateDetail.test.tsx`; `src/contexts/AppContext.test.tsx` | `npm run qa:coverage` | Automated | Pending updates render, apply calls the backend, success/failure toasts show, and stale banners are filtered. |  |  |
| I014 | Mod Library | Installed library search, sort, grouping, version selection, tags, empty states, and large lists remain stable. | mod-library, empty-state, large-list | `src/components/Select.test.tsx`; `src/components/LibraryTable.test.tsx`; `src/components/LibraryRow.test.tsx`; `src/views/Mods.test.tsx`; `src/hooks/usePinScroll.test.tsx`; `src/lib/modGrouping.test.ts`; `src-tauri/src/mod_versions.rs::local_options_use_source_version_and_uppercase_v_for_cached_updates`; `src-tauri/src/mod_versions.rs::source_version_cache_keeps_same_manifest_nexus_update_selectable`; `src-tauri/src/mod_versions.rs::readable_nexus_filename_version_suppresses_update_pill`; `src-tauri/src/mod_versions.rs::batched_local_version_options_match_per_target_for_large_library`; `src-tauri/src/mod_versions.rs::prune_cached_versions_keeps_installed_profile_used_and_selected_versions` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Rows are searchable and sortable, Updates first keeps all rows visible, installed and cached versions collapse behind a clearly labeled themed selector even when display names drift, newly cached update versions appear in the Library selector immediately, version switches keep scroll anchored, same-manifest and readable-filename Nexus source versions stay selectable, large libraries batch version-option work, unused cached versions are pruned, empty states render, and 100-plus-mod cases keep controls stable. |  |  |
| I015 | Mod Library | Toggle on/off moves files between active and disabled storage. | mod-library, profile-flow | `qa/runner/smoke.mjs::specToggleMovesQaTestModToDisabled`; `src-tauri/src/mods/mod.rs::disabling_one_same_named_mod_leaves_the_other_active` | `npm run qa:smoke`; `npm run qa:rust` | Automated | UI state and disk storage agree after toggling, including same-name folder identity. |  |  |
| I016 | Mod Library | Row kebab menu opens, supports keyboard interaction, and can route to menu customization. | mod-library, menu, settings | `src/components/KebabMenu.test.tsx`; `src/contexts/RowMenuContext.test.tsx`; `src/App.test.tsx` | `npm run qa:coverage` | Automated | Menu opens/closes accessibly and Customize menu routes to Settings. |  |  |
| I017 | Mod Library | Pin or source editing preserves source identity, display-name overrides, displayed source versions, and visible source state. | mod-library, dialog, error-state | `src/components/SourceEditor.test.tsx`; `src/views/Mods.test.tsx`; `src/components/LibraryRow.test.tsx`; `src/components/LibraryTable.test.tsx`; `src/components/ModpackDetail.test.tsx`; `src/lib/githubLinks.test.ts`; `qa/runner/smoke.mjs::specDisplayNameOverrideUpdatesRow`; `qa/runner/smoke.mjs::specAutoDetectedGitHubSavePromotesSource`; `qa/runner/smoke.mjs::specStaleManifestSourceVersionDisplaysInstalledTag` | `npm run qa:coverage`; `npm run qa:smoke`; `npm run qa:smoke:cassette` | Automated | GitHub, Nexus, manual and auto-detected source states, source-installed versus manifest versions, and manager-only display names render after save; save failures are visible. |  |  |
| I018 | Mod Library | Single-mod update downloads save a selectable version only when policy allows it. | mod-library, error-state | `src/views/Mods.test.tsx`; `src/hooks/useModLibrary.test.tsx`; `src/lib/installPolicy.test.ts`; `src-tauri/src/updater.rs::first_versioned_release_with_mod_assets_skips_empty_and_non_mod_releases`; `src-tauri/src/mod_versions.rs::cache_restore_uses_artifact_id`; `src-tauri/src/mod_versions.rs::local_options_use_source_version_and_uppercase_v_for_cached_updates`; `src-tauri/src/mod_versions.rs::source_version_cache_keeps_same_manifest_nexus_update_selectable`; `src-tauri/src/profiles/membership.rs::membership_matrix_does_not_move_pinned_profile_to_newer_installed_artifact` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Download update pills call `update_mod`, blocked Nexus or no-asset states do not create a bad affordance, downloaded versions appear in Versions without changing the active library copy or saved modpacks, same-manifest Nexus file versions still appear, saved modpacks stay pinned to their selected artifact, and failures toast. |  |  |
| I019 | Mod Library | Audit and bulk update flows cache results, refresh rows by artifact identity, and recover from failures. | mod-library, error-state, large-list | `src/contexts/AppContext.test.tsx`; `src/lib/auditState.test.ts`; `src-tauri/src/updater.rs::last_installed_source_suppresses_alternate_update_channel`; `src-tauri/src/updater.rs::nexus_source_version_wins_over_stale_manifest_version`; `src-tauri/src/updater.rs::walk_back_candidate_gate_rejects_dev_builds_and_empty_releases`; `qa/runner/smoke.mjs::specAuditAgainstCassettesShowsOnePending`; `src-tauri/src/subscriptions.rs::diff_profiles_keeps_same_named_artifacts_distinct` | `npm run qa:coverage`; `npm run qa:smoke:cassette`; `npm run qa:rust` | Automated | Pending counts are visible, scoped refresh merges every stable row identity, Nexus-only update pills stay out of GitHub bulk updates, the last installed source and source-tracked Nexus version suppress stale alternate-source update pills, same-named subscribed artifacts stay distinct, failures keep prior results, and cassette audit reports one update. |  |  |
| I020 | Mod Library | Auto-detect sources modal handles scanning, skip toggles, rate limits, partial failures, and empty states. | mod-library, dialog, error-state, empty-state | `src/components/AutoDetectModal.test.tsx` | `npm run qa:coverage` | Automated | High-confidence rows can be skipped, empty already-linked states render, rate limits are visible, and apply failures toast. |  |  |
| I021 | Mod Library | Quick Add handles GitHub URLs, Nexus URLs, sticky Nexus guidance, failed installs, and incompatible downloaded versions. | mod-library, dialog, error-state, settings | `src/components/QuickAddModal.test.tsx`; `src/views/Help.test.tsx`; `src/lib/nexusUrl.test.ts`; `src/App.test.tsx`; `src-tauri/src/downloads_watcher.rs::compatibility_payload_tests` | `npm run qa:coverage`; `npm run qa:rust` | Automated | GitHub and Nexus inputs route correctly, missing Nexus-key paths guide to Settings, failures are visible, and downloaded mods that need a newer game show a warning instead of a success toast. |  |  |
| I022 | Mod Library | File import and Add Mods menu expose Quick Add and local import without bypassing game-running guards. | mod-library, menu, error-state | `src/hooks/useModLibrary.test.tsx`; `src/components/AddModsMenu.tsx`; `src/App.test.tsx` | `npm run qa:coverage` | Automated | Import actions call the picker or install path, game-running guards disable unsafe actions, and errors show. |  |  |
| I023 | Mod Library | Delete removes the row and files after confirmation. | mod-library, menu, dialog | `qa/runner/smoke.mjs::specDeleteUpToDateMod`; `src/components/LibraryRow.test.tsx` | `npm run qa:smoke`; `npm run qa:coverage` | Automated | Confirmed delete removes the visible row and on-disk folder. |  |  |
| I024 | Mod Library | Bulk enable, disable, delete, and failure states are visible and safe for many mods. | mod-library, dialog, error-state, large-list | `src/views/Mods.test.tsx`; `src-tauri/src/mods/mod.rs::delete_all_mods_does_not_rewrite_active_profile_manifest_to_empty` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Bulk command wiring, game-running locks, failure toasts, and backend delete safety are covered. |  |  |
| I025 | Browse Mods | Public mod browser search, detail, install, missing Nexus key, and empty results are owned. | mod-library, empty-state, settings, error-state | `src/views/Browse.test.tsx`; `src/components/BrowseDetail.test.tsx` | `npm run qa:coverage` | Automated | Browse search and detail views render, settings CTA opens when the Nexus key is missing, and empty states are visible. |  |  |
| I026 | Modpacks | Modpack list, tabs, search, active selection, empty states, and large counts stay usable. | profile-flow, empty-state, large-list | `src/views/Profiles.test.tsx`; `src/components/ModpackDetail.test.tsx` | `npm run qa:coverage` | Automated | Yours/Browse tabs and large modpack lists keep selection and visible state stable. |  |  |
| I027 | Modpacks | Create Modpack wizard builds packs from active, empty, or selected mods. | profile-flow, dialog, mod-library, empty-state, large-list | `src/components/CreateModpackWizard.test.tsx`; `qa/runner/smoke.mjs::specCreateModpack` | `npm run qa:coverage`; `npm run qa:smoke` | Automated | Wizard steps validate input, incompatible active mods stay out, and the saved pack appears. |  |  |
| I028 | Modpacks | Switch, apply, display, or save load order preserves pins, sticky toggle state, and recoverable game settings. | profile-flow, mod-library | `qa/runner/smoke.mjs::specModpackSwitchPreservesFreeze`; `qa/runner/smoke.mjs::specToggleStickyAcrossModpackSwitch`; `src/components/LibraryTable.test.tsx::defaults pack-scoped rows to the saved load order`; `src-tauri/tests/qa_scenarios.rs::flow_11_apply_profile_pins_override_profile_state`; `src-tauri/src/profiles/membership.rs::write_profile_load_order_to_settings_save_repairs_trailing_fragment` | `npm run qa:smoke`; `npm run qa:coverage`; `npm run qa:rust` | Automated | Switching preserves frozen mods, disabled state stays sticky, pinned files remain enabled, modpack rows display in saved load order, and load order saves repair stale trailing settings data. |  |  |
| I029 | Modpacks | Share publish and setup flows route through GitHub setup, publish, copy, and error states. | profile-flow, dialog, error-state | `src/components/PublishModal.test.tsx`; `src/components/ShareSetupPanel.test.tsx`; `src-tauri/src/sharing/mod.rs::share_profile_routes_bundles_through_releases_and_persists_hash`; `src-tauri/src/sharing/mod.rs::signature_ignores_mod_version_ids`; `src-tauri/src/sharing/mod.rs::signature_changes_on_real_field_change` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Setup later routes to Settings, publish state is visible, copied share codes work, backend share persistence is tested, ID migration alone does not mark owned packs out of sync, and changed artifact content does. |  |  |
| I030 | Modpacks | Import share code from the Modpacks toolbar focuses, parses, and applies the same smart outcomes as Home. | profile-flow, dialog, error-state | `src/views/Profiles.test.tsx`; `src/lib/shareImport.smart.test.tsx` | `npm run qa:coverage` | Automated | Toolbar quick-add accepts array or scalar picker output and smart routing outcomes match Home. |  |  |
| I031 | Modpacks | Modpack detail add/remove membership matrix handles many mods, manager tags, and same-source mods without losing scroll or focus. | profile-flow, mod-library, menu, large-list | `src/components/ModpackDetail.test.tsx`; `src/components/ModMultiSelect.test.tsx`; `src/hooks/usePinScroll.test.tsx`; `src-tauri/src/profiles/membership.rs::set_profile_mod_membership_keeps_same_nexus_page_variants_distinct`; `src-tauri/src/profiles/drift.rs::registry_drift_matches_added_library_mod_by_artifact_id_when_identity_fields_drift`; `src-tauri/src/profiles/crud.rs::artifact_match_uses_profile_identity_for_conflicting_shared_id`; `src-tauri/src/mod_versions.rs::batched_local_version_options_match_per_target_for_large_library` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Membership toggles keep focus/scroll, Add Mods menu routes correctly, No tags filters pack rows, same Nexus-page mods stay distinct, Add from Library does not leave a false unsaved-changes banner, conflicting shared artifact IDs resolve by content, large selections stay bounded, and local version options are batched for large libraries. |  |  |
| I032 | Modpacks | Repair and missing-bundle recovery show visible outcomes and compatible walk-back behavior. | profile-flow, error-state | `qa/runner/smoke.mjs::specRepairWalkback`; `src/components/MissingBundlesPanel.test.tsx` | `npm run qa:smoke:cassette`; `npm run qa:coverage` | Automated | Repair installs a compatible release, missing bundle states render, and failed downloads are visible. |  |  |
| I033 | Modpacks | Rename, edit, duplicate, delete, and advanced actions are dialog-owned. | profile-flow, dialog, menu, error-state | `src/components/RenameModpackModal.test.tsx`; `src/components/EditModpackModal.test.tsx`; `src/components/ModpackDetail.test.tsx`; `src/components/AdvancedSection.test.tsx` | `npm run qa:coverage` | Automated | Confirm/cancel paths, validation, persistence, and failure states are covered. |  |  |
| I034 | Browse Modpacks | Public modpack browser detail, subscribe/import, empty CTA, and return-to-yours flows are owned. | profile-flow, empty-state, error-state | `src/views/BrowseModpacks.test.tsx`; `src/components/BrowseModpackDetail.test.tsx`; `src/App.test.tsx` | `npm run qa:coverage` | Automated | Empty browse CTA returns to Yours, detail subscribe/import routes correctly, and errors remain visible. |  |  |
| I035 | Settings | Game path, autodetect, manual path, validation, and unsupported path failures are owned below OS registration. | settings, dialog, empty-state, error-state | `src/views/Settings.test.tsx`; `src/components/AutoDetectModal.test.tsx`; `src-tauri/src/game.rs::libraryfolders_vdf_unescapes_and_discovers_alt_library_game` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Settings General renders missing/detected states, autodetect failures toast, and alternate Steam library parsing is tested. |  |  |
| I036 | Settings | Theme, language, UI scale, sidebar width, navigation layout, and layout preferences persist safely. | settings, large-list | `src/components/ThemeSelect.test.tsx`; `src/components/LanguageSelect.test.tsx`; `src/components/UiScaleSlider.test.tsx`; `src/display/UiScaleContext.test.tsx`; `src/display/navigationLayout.test.ts`; `src/hooks/useResizableSidebar.test.tsx`; `src/views/Settings.test.tsx` | `npm run qa:coverage` | Automated | Preferences save, clamp, degrade on hostile storage, broadcast navigation layout changes, and apply to the rendered shell. |  |  |
| I037 | Settings | Row menu customizer changes menu composition and survives storage failures. | settings, menu, error-state | `src/components/RowMenuCustomizer.test.tsx`; `src/contexts/RowMenuContext.test.tsx`; `src/lib/rowMenuConfig.test.ts` | `npm run qa:coverage` | Automated | Required items remain available, user choices persist, and storage failures degrade safely. |  |  |
| I038 | Settings | Backup restore confirmation, restore command, and filesystem restore behavior are covered. | settings, dialog, error-state | `src/views/Settings.test.tsx`; `src-tauri/src/backup.rs::restore_backup_restores_config_files` | `npm run qa:coverage`; `npm run qa:rust` | Automated | Restore requires confirmation, invokes `restore_backup_cmd`, and backend restore rolls files back. |  |  |
| I039 | Settings | Logs viewer, diagnostic bundle, Report a bug, and feedback links expose support paths without leaking secrets. | settings, dialog, error-state | `src/components/DiagnosticBundle.test.tsx`; `src/components/LogsViewer.test.tsx`; `src/components/AboutCard.test.tsx`; `src/hooks/useOpenFeedback.test.tsx` | `npm run qa:coverage` | Automated | Bundle modal opens/closes, logs render, feedback opens, and failure paths are visible. |  |  |
| I040 | Settings | About card update check, app version fallback, author link, and feedback affordances are owned. | settings, error-state | `src/components/AboutCard.test.tsx` | `npm run qa:coverage` | Automated | Already-latest, update available, checking, install, feedback, and non-Error failures are visible. |  |  |
| I041 | Settings | Help tab and Help drawer content cover Nexus, Discord paste, and onboarding help paths. | settings, empty-state | `src/views/Help.test.tsx`; `src/components/HelpDrawer.test.tsx`; `src/components/HelpHint.test.tsx` | `npm run qa:coverage` | Automated | Help content renders on both surfaces and key CTA copy is present. |  |  |
| I042 | Onboarding | First-launch player and creator paths, skip, retry detect, and final CTAs are owned. | dialog, empty-state, settings, profile-flow | `src/components/OnboardingOverlay.test.tsx`; `src/App.test.tsx` | `npm run qa:coverage` | Automated | Onboarding appears once, skip persists, no-game retry does not persist, and CTAs route to Home or Create Modpack. |  |  |
| I043 | Localization | Supported locale parity and copied-English guards cover player-visible text. | global-shell, settings, error-state | `src/i18n/locales/parity.test.ts`; `src/i18n/locales/plurals.test.ts`; `src/i18n/language.test.ts` | `npm run qa:i18n`; `npm run qa:coverage` | Automated | Locale keys stay in sync, plurals remain valid, and language fallback routing is covered. |  |  |
| I044 | Release notes | Changelog parsing, translation lookup, and What's New rendering keep release notes visible. | global-shell, dialog, empty-state | `src/lib/changelog.test.ts`; `src/components/WhatsNewCard.test.ts`; `src/components/WhatsNewCard.component.test.tsx`; `src/components/WhatsNewCard.translated.test.tsx` | `npm run qa:coverage` | Automated | Latest release notes parse, translated bodies render when available, and empty/unreleased cases degrade safely. |  |  |
| I045 | QA gate | Coverage matrix and interaction inventory completeness fail on missing owners. | global-shell, error-state | `scripts/qa-coverage-matrix.test.mjs`; `scripts/qa-coverage-matrix.mjs` | `npm run qa:matrix`; `node --test scripts/qa-coverage-matrix.test.mjs` | Automated | CI logs report inventory counts, unowned interactions fail, and manual rows require reasons plus review dates. |  |  |
| I046 | App shell | Renderer crashes show a recovery panel and write diagnostics to the app log instead of blanking the window. | global-shell, error-state | `src/components/AppErrorBoundary.test.tsx`; `src/hooks/useTauri.test.ts` | `npm run qa:coverage`; `npm run qa:unit -- --run src/components/AppErrorBoundary.test.tsx src/hooks/useTauri.test.ts` | Automated | Render crashes and global async errors are visible, logged, and do not leave the user staring at a blank WebView. |  |  |
| I047 | App shell | Windows update downloads install over the same app copy the user launched. | global-shell, error-state | `scripts/release-windows-installer-policy.test.mjs`; `src-tauri/src/app_update.rs`; `src-tauri/nsis-hooks.nsh`; `src-tauri/tauri.conf.json`; `.github/workflows/build.yml`; `scripts/publish-updater.sh` | `node --test scripts/release-windows-installer-policy.test.mjs`; `npm run qa:matrix` | Automated | Release metadata points Windows users at NSIS/current-user setup, the update command passes the current app folder to NSIS, the setup EXE corrects stale metadata only for older 1.7 launches without `/D=`, and no MSI fallback path is published. |  |  |
| M001 | Native desktop | Drag a local mod archive from the OS onto a packaged app window. | global-shell, mod-library, manual-only | Manual release spot-check | Manual release spot-check | Manual | Confirm the packaged app receives the OS-level drop and install starts. | Tauri intercepts native drag/drop before Selenium can synthesize the OS event. | 2026-09-01 |
| M002 | Native desktop | Click an external `sts2mm://` link on Windows or Linux and confirm app registration plus warm-start focus. | global-shell, profile-flow, dialog, manual-only | Manual release spot-check | Manual release spot-check | Manual | Confirm external protocol registration, existing-window focus, and one confirm dialog. | The automated tests own React routing and dedupe, not OS protocol registration. | 2026-09-01 |
| M003 | Native desktop | Launch the real game through Steam protocol and direct binary launch modes. | global-shell, manual-only | Manual release spot-check | Manual release spot-check | Manual | Confirm the selected launch mode starts the game from the maintainer release machine. | Real Steam/process launch is an OS and local-install boundary. | 2026-09-01 |
| M004 | Native desktop | Smoke a packaged macOS build after OS-divergent changes. | settings, profile-flow, mod-library, manual-only | Manual release spot-check | Manual release spot-check | Manual | Confirm game path, switch, drag/drop, toggle, and support bundle on macOS. | Apple provides no WebDriver path for the embedded WKWebView used by Tauri. | 2026-09-01 |
| M005 | Native desktop | Do a short visual layout pass on Windows and Linux after the WebDriver smoke passes. | global-shell, settings, manual-only | Manual release spot-check | Manual release spot-check | Manual | Confirm no obvious clipping, overlap, or unreadable high-density lists in the packaged UI. | Screenshot and component tests catch many states, but final visual feel remains a human review boundary. | 2026-09-01 |
